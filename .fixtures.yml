# This file can be used to install module dependencies for unit testing
# See https://github.com/puppetlabs/puppetlabs_spec_helper#using-fixtures for details
---
fixtures:
  symlinks:
    pxe_install: "#{source_dir}"
  repositories:
    provision: 
      repo: 'git@github.com:tom-krieger/provision.git'
      branch: 'sudo_secure_path_patch'
  forge_modules:
    stdlib:
      repo: "puppetlabs/stdlib"
      ref: "6.0.0"
    concat:
      repo: "puppetlabs/concat"
      ref: "6.0.0"
    echo:
      repo: "ipcrm/echo"
      ref: "0.1.6"
    dhcp:
      repo: "puppet/dhcp"
      ref: "4.0.0"
    puppet_agent:
      repo: "puppetlabs/puppet_agent"
      ref: "3.0.2"
    facts:
      repo: "puppetlabs/facts"
      ref: "1.0.0"
    apache:
      repo: "puppetlabs/apache"
      ref: "5.5.0"

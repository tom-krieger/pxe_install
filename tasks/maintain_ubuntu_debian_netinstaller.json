{
    "description": "Maintain Ubuntu/Debian net installer",
    "input_method": "environment",
    "supports_noop": false,
    "parameters": {
      "tftp_basedir": {
        "description": "The base directory of the tftpd server.",
        "type": "String[1]",
        "default": "/var/lib/tftpboot"
      },
      "archive": {
          "description": "The URL to download the net installer archive from.",
          "type": "String[1]"
      },
      "os": {
        "description": "The operating system",
        "type": "Enum[ubuntu,debian]"
      },
      "os_version": {
        "description": "The os version",
        "type": "String"
      }
    }
  }
 